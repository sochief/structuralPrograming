#include <stdio.h>
#include <string.h>
#include <dos.h>
#include <stdlib.h>
#include <stdbool.h>
#include "class.h"
// !TODO get time in string and call it every time we create a relation.
int main()
{
    // get input, test functions for student.c
    StudList *studList;
    char line[100];
    char element;
    int i;
    char *token, *delim;
    FILE *fptr;

    studList = (StudList *)malloc(sizeof(StudList));
}
